"use strict";
// Direction is the name of the enum.
// Up, Down, Left, and Right are the named constants within the enum.
// The value of Up is explicitly set to 1. The following members (Down, Left, Right) are auto-incremented from this starting value, resulting in values 2, 3, and 4, respectively.
// enum Direction {
// Up = 1,
// Down,
// Right,
// Left, 
// }
// let dir: Direction = Direction.Up
// console.log(dir);
// enum Coffeetype {
// Latte,
// Cappicinu,
// Mooca,
// Flatewhite
// }
// let coffe: Coffeetype = Coffeetype.Cappicinu;
// console.log(coffe);
// Loops
for (let abc = 10; abc > 0; abc--) {
    console.log(abc);
}
;
